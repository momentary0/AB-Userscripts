## AnimeBytes Torrent Sorter

Sorts anime torrents in order of quality.

The script in `src` requires ES2015 features. The scripts inside `dist` do
not. Of the two scripts inside `dist`, one is minified and optimised.